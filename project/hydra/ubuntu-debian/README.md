# Ubuntu + Debian

Project Hydra lane for Linux studio hosts, Ubuntu/Debian services, toolchains, local compute, build support, and reproducible workstation notes.

This lane does not own Samsung runtime truth. Physical Android behavior must be proven on the intended Samsung device.

Rules: no secrets in Git, reference rather than duplicate canonical material, and preserve rollback before disruptive host changes.
