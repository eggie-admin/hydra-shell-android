# LuHm OS GUI Mockup Workflow

Seal: `KAI9000_LUHM_GUI_MOCKUP_WORKFLOW_20260917`

Status: `MOCKUP_ONLY / CROSSING_AMBER / NO_RUNTIME_AUTHORITY`

This lane is a reversible presentation mockup derived from the current private Crown doctrine. It does not replace the canonical Android runtime, compile an APK, install anything, publish publicly, or grant machine authority.

## Current doctrine mapped into the mockup

- One user-facing parent: **Lum Fast**.
- Mini helpers are **read/search only** and never gain mutation authority through consensus.
- Presentation scales: `SPRITE_BUBBLE -> WIDGET_DECK -> FULL_COCKPIT` with one conceptual shared session/state tree.
- Realms: `GAME / ADMIN / SYSTEM`, with the GAME -> ADMIN authority firewall visible and absolute.
- Tiers: `WHISPER / CROSSING / RECKONING`.
- Evidence law: **AI proposes. Policy authorizes. CI proves. The human promotes.**
- Mutation presentation is gated. The local mutation gateway remains outside this mockup and is never called by browser code.
- Public publish is OFF. APK promotion/install is OFF for this workflow.

## Files

- `index.html` provides the semantic shell.
- `mockup.css` provides portrait-first responsive presentation.
- `mockup.js` switches only local presentation state. It performs no network, shell, file write, install, publish, or mutation action.
- `mockup-contract.json` records the bounded state and authority contract.

## Mockup acceptance gate

The source mockup is GREEN only when the focused source gate proves required doctrine labels, valid JSON, JavaScript syntax, and absence of network/execution escape hatches. Visual/device approval remains a separate Professor review.

Do not merge this lane merely because source CI is GREEN.