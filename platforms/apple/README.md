# Apple lane

Status: STAGED DUMMY REPOSITORY CONTRACT

Future scope: iOS, iPadOS, and macOS shell/distribution work.

Planned boundary:
- shared Python/API contracts where Apple platform rules permit
- native lifecycle, entitlements, permissions, signing, notarization, and App Store packaging remain Apple-specific
- no claim of a buildable Xcode project yet
- no Apple signing credentials or provisioning profiles in Git

Promotion requirement: replace this placeholder with a real build project, CI, signing strategy, supported OS matrix, and installable artifact evidence before leaving `luhmos/testing`.
