name: LuHm OS GitHub Beta Publisher

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  dispatch-registration-only:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Default-branch dispatch registration only. Canonical workflow executes from requested ref.'
