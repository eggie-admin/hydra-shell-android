name: LuHm Cathedral Legacy One-Click Guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Legacy lane retired
        shell: bash
        run: |
          set -euo pipefail
          echo 'The old Cathedral one-click build lane is retired.'
          echo 'Canonical signed APK workflow: .github/workflows/luhmos-crown-gate-apk.yml'
          echo 'No build, signing, publication, or installation side effect occurs here.'
          echo 'KAI9000_LEGACY_ONECLICK_RETIRED_GREEN'
