[project]
name = "kai9000-ultima-ollama-ffmpeg-antenna"
version = "3.0.0"
description = "Local Ollama + FFmpeg antenna with GitHub remote and Drive recovery mirror"
requires-python = ">=3.10"
dependencies = ["httpx"]
