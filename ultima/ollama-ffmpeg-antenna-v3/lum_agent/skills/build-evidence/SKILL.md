# Build Evidence

Keep evidence layers separate: source contract, compile/test, CI run, artifact existence, package identity, signing identity, alignment/ABI checks, install, launch, update continuity, and physical-device behavior. A lower layer never proves a higher one. Record commit/ref, run ID, artifact/hash, and explicit pending gates without secrets.
