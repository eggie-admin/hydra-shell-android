repo_name: KAI 9000 / LuHm OS Testing
repo_description: >-
  Private LuHm OS / KAI 9000 Samsung Android testing repository.
  Generated by fdroidserver from a verified KAI 9000 APK and package metadata.
repo_url: https://REPLACE_WITH_HOST/fdroid/repo/
repo_icon: icon.png

repo_keyalias: kai9000-fdroid-repo
keystore: {env: FDROID_KEYSTORE}
keystorepass: {env: FDROID_KEYSTORE_PASS}
keypass: {env: FDROID_KEY_PASS}
keydname: CN=KAI 9000 F-Droid Repo, OU=LuHm OS, O=EggieBagelFace, C=US

archive_older: 0
make_current_version_link: true

# This file intentionally contains no secrets.
# The actual keystore path and passwords are supplied by a trusted signing host
# or protected CI secret boundary at runtime.
